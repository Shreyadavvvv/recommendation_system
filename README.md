# recommendation_system
This project builds a movie recommender using the MovieLens 100K dataset and the Surprise library. It uses Singular Value Decomposition (SVD) to predict user ratings and recommends unseen movies based on learned user preferences.
